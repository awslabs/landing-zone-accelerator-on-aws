{
  "name": "@aws-compliant-constructs/secure-vpc",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "author": {
    "name": "Amazon Web Services",
    "url": "https://aws.amazon.com"
  },
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "cleanup": "tsc --build ./ --clean && rm -rf node_modules && rm -rf yarn.lock",
    "cleanup:tsc": "tsc --build ./ --clean",
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.102.0",
    "@types/jest": "^26.0.10",
    "@types/node": "10.17.27",
    "jest": "^26.4.2",
    "ts-jest": "^26.2.0",
    "typescript": "~3.9.7"
  },
  "peerDependencies": {
    "@aws-cdk/core": "1.102.0"
  },
  "dependencies": {
    "@aws-cdk/aws-ssm": "1.102.0",
    "@aws-cdk/core": "1.102.0"
  }
}
